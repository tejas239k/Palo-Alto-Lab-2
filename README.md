# Palo-Alto-Lab-2
Configured TAP INTERFACE Tap interface is a passive listening mode in Palo Alto used to monitor network traffic without interfering/preventing with the live network flow. Also configured V-Wire Two interfaces are logically bound together to function as a single, transparent "bump in the wire"
